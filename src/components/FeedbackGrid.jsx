import React from 'react';

export default function FeedbackGrid(props) {
  // Classic feedback grid logic/UI will go here
  return (
    <div>
      {/* Feedback grid UI */}
      {props.children}
    </div>
  );
}
